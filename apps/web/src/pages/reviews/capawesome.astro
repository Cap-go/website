---
import ConversionLandingLayout from '@/components/conversion/ConversionLandingLayout.astro'
import { getRelativeLocaleUrl } from '@/services/links'
import m from '@/copy/messages'
import { createServiceLdJson, createLdJsonGraph } from '@/lib/ldJson'

const locale = Astro.locals.locale
const title = m.review_capawesome_title({}, { locale })
const description = m.review_capawesome_meta_description({}, { locale })

const serviceLdJson = createServiceLdJson(Astro.locals.runtimeConfig.public, {
  name: title,
  description,
  url: Astro.url.href,
  serviceType: 'Software Development Service',
  areaServed: ['Worldwide'],
})

const ldJSON = createLdJsonGraph(Astro.locals.runtimeConfig.public, serviceLdJson, {
  includeOrganization: true,
})

const content = { title, description, ldJSON, image: `${Astro.locals.runtimeConfig.public.baseUrl}/capgo_social.png` }

const relatedLinks = [
  { label: m.capwesome_title({}, { locale }), href: getRelativeLocaleUrl(locale, 'alternatives/capawesome') },
  { label: m.migrate_from_capawesome_title({}, { locale }), href: getRelativeLocaleUrl(locale, 'migrate/from-capawesome') },
  { label: 'Upgrade guide (docs)', href: getRelativeLocaleUrl(locale, 'docs/upgrade/from-capawesome-to-capgo') },
  { label: 'Capawesome alternative (blog)', href: getRelativeLocaleUrl(locale, 'blog/alternative-to-capawesome') },
]
---

<ConversionLandingLayout
  locale={locale}
  content={content}
  heroTitle={m.review_capawesome_hero_title({}, { locale })}
  heroSubtitle={m.review_capawesome_hero_subtitle({}, { locale })}
  sections={[
    { title: m.review_capawesome_what_title({}, { locale }), paragraphs: [m.review_capawesome_what_p1({}, { locale })] },
    { title: m.review_capawesome_capgo_title({}, { locale }), paragraphs: [m.review_capawesome_capgo_p1({}, { locale })] },
  ]}
  relatedLinks={relatedLinks}
/>
