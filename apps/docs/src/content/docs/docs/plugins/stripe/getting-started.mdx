---
title: Getting Started
description: "Install @capgo/capacitor-stripe and start using its Capacitor API."
sidebar:
  order: 2
---

## Install

```bash
bun add @capgo/capacitor-stripe
bunx cap sync
```

## Import

```typescript
import { Stripe } from '@capgo/capacitor-stripe';
```

## Subscriptions and in-app purchases

For App Store / Play Store subscriptions and entitlements, use [@capgo/native-purchases](/docs/plugins/native-purchases/) instead of this plugin.

## Payment Sheet flow

```typescript
await Stripe.initialize({ publishableKey: 'pk_test_...' });

await Stripe.createPaymentSheet({
  paymentIntentClientSecret: 'pi_..._secret_...',
  merchantDisplayName: 'My Store',
});

const result = await Stripe.presentPaymentSheet();
```

## Direct Charges (Stripe Connect)

Pass `stripeAccount` on `initialize()` or per `createPaymentSheet()` when creating PaymentIntents on a connected account.

## Example app

```bash
git clone https://github.com/Cap-go/capacitor-stripe.git
cd capacitor-stripe/example-app
bun install
bun run start
```


## Source Of Truth

This page documents the current public API from [Cap-go/capacitor-stripe](https://github.com/Cap-go/capacitor-stripe/).
