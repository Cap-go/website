---
title: "@capgo/capacitor-device-info"
description: "Read CPU, memory, GPU, storage, thermal, and onboard sensor metrics from Capacitor apps."
tableOfContents: false
next: false
prev: false
sidebar:
  order: 1
  label: "Introduction"
hero:
  tagline: "Device metrics and onboard sensor snapshots for dashboards, diagnostics, and runtime monitoring."
  actions:
    - text: Get started
      link: /docs/plugins/device-info/getting-started/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/Cap-go/capacitor-device-info/
      icon: external
      variant: minimal
---

## Overview

`@capgo/capacitor-device-info` reads device health and hardware metrics from Capacitor apps. It can return one snapshot on demand or stream periodic updates through `deviceInfoUpdate` listeners for charts and operational dashboards.

## Compatibility

The plugin requires Capacitor 8 or later (`@capacitor/core >=8.0.0`).

## Core capabilities

- `getInfo` - Read CPU, memory, GPU, storage, thermal state, low-power mode, and onboard sensor data.
- `startMonitoring` - Start interval sampling and emit `deviceInfoUpdate` events.
- `stopMonitoring` - Stop the active monitoring session.
- `isMonitoring` - Inspect the active monitoring state and sample count.
- `removeAllListeners` - Remove all listeners registered on the plugin.
- `getPluginVersion` - Return the native plugin version.

## Onboard sensor data

The plugin reports only hardware data exposed by the device or OS. It does not call weather services or fetch outside temperature or humidity from the network.

Android can report available sensors and common readings such as battery temperature, ambient temperature, relative humidity, pressure, light, and proximity when the hardware exposes them. Android CPU and GPU temperature are best-effort thermal-zone reads and can be omitted by restricted devices.

iOS reports CPU, memory, storage, GPU, thermal state, low-power mode, and CoreMotion sensor availability. Public iOS APIs do not expose raw CPU or GPU temperature.

## Source of truth

The full API reference follows [`src/definitions.ts`](https://github.com/Cap-go/capacitor-device-info/blob/main/src/definitions.ts) in the plugin repository. See [Getting Started](/docs/plugins/device-info/getting-started/) for install, monitoring behavior, return types, and examples.

## Keep going from @capgo/capacitor-device-info

If you are using **@capgo/capacitor-device-info** for diagnostics or monitoring, connect it with [Getting Started](/docs/plugins/device-info/getting-started/) for install and usage, [Using @capgo/capacitor-device-info](/plugins/capacitor-device-info/) for the tutorial page, [@capgo/capacitor-barometer](/docs/plugins/barometer/) for dedicated pressure streaming, and [@capgo/capacitor-light-sensor](/docs/plugins/light-sensor/) for dedicated light sensor updates.
