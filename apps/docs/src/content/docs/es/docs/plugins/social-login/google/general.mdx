---
title: Google Configuración de inicio de sesión
description: ''
sidebar:
  order: 1
locale: es
---
import { Steps } from '@astrojs/starlight/components';

## Introducción

En esta guía, aprenderá cómo configurar el inicio de sesión Google con el inicio de sesión social Capgo. Necesitará lo siguiente para configurar Google Iniciar sesión:

- Una cuenta Google

## Configuración general

:::note
Este paso es necesario independientemente de la plataforma que decida utilizar. 
:::

En esta parte, configurará la pantalla de inicio de sesión que muestra Google.

<Steps>
    1. Vaya a [console.cloud.google.com](https://console.cloud.google.com/)
    2. Haga clic en el selector de proyectos.
        <img src="/social-login-assets/google_cons_project_selector.png" alt="Google Selector de proyectos de consola" />
    3. Si aún no tienes un proyecto, **crea un proyecto nuevo**.
        1. Haga clic en `Nuevo proyecto`
            <img src="/social-login-assets/google_cons_new_project_btn.png" alt="" />
        2. Asigne un nombre a su proyecto y haga clic en `Create`.
            <img src="/social-login-assets/google_cons_name_projec.png" alt="Pantalla de nomenclatura del proyecto que muestra el campo de nombre y el botón Crear" />
        3. Asegúrese de estar en el proyecto correcto
            <img src="/social-login-assets/google_cons_right_proj.png" alt="El nombre del proyecto se muestra en el selector, lo que indica la selección correcta del proyecto." />
    4. Comience a configurar el `OAuth consent screen`
        1. Haga clic en la barra de búsqueda
            <img src="/social-login-assets/google_cons_search.png" alt="Google Barra de búsqueda de la consola" />
        2. Busque `OAuth consent screen` y haga clic en él.
            <img src="/social-login-assets/google_cons_search_2.png" alt="Los resultados de la búsqueda muestran la opción de pantalla de consentimiento OAuth" />
        3. Configurar la pantalla de consentimiento
            :::nota
            Asumiré que estás desarrollando una aplicación abierta al público, por lo que usaré el tipo de usuario `external`. Seleccione el tipo de usuario que más le convenga Y haga clic en `create`
            :::

            Haga clic en `create`
            <img src="/social-login-assets/google_cons_oauth_const_scr.png" alt="OAuth selección del tipo de usuario de la pantalla de consentimiento con opciones externas e internas" />
    5. Completa la información sobre tu aplicación.
            1. Comencemos con la "Información de la aplicación".
                <img src="/social-login-assets/google_cons_app_inf.png" alt="Sección de información de la aplicación que muestra el nombre de la aplicación y los campos de correo electrónico de soporte al usuario" />
                
                    - Por favor escriba su `Nombre de la aplicación`
                        :::precaución
                        **ESTO SERÁ MOSTRADO A LOS USUARIOS**
                        :::
                    - Ingrese el `correo electrónico de soporte al usuario`
                        :::nota
                        Puede obtener más información sobre el correo electrónico de soporte [aquí](https://support.google.com/cloud/answer/10311615#user-support-email&zippy=%2Cuser-support-email/)
                        :::
                2. **PUEDES** agregar el logotipo de la aplicación.
                    <img src="/social-login-assets/google_cons_app_logo.png" alt="" />
                    :::nota
                    Esto no es obligatorio y me saltaré este paso.
                    :::
                3. **DEBE** configurar el `Dominio de la aplicación`
                    <img src="/social-login-assets/google_cons_app_doma.png" alt="Sección de configuración del dominio de la aplicación con campo de dominios autorizados" />
                    :::nota
                    No haré eso porque es solo una demostración simple que **NO** se publicará, pero recomiendo encarecidamente completar esta sección.
                    :::
                    
                4. **TIENES QUE** proporcionar el correo electrónico del desarrollador.
                    <img src="/social-login-assets/google_cons_dev_cont_inf.png" alt="Sección de información de contacto del desarrollador con campo de correo electrónico" />     
                5. Haga clic en `guardar y continuar`
                        <img src="/social-login-assets/google_cons_cons_sav_cont.png" alt="Botón Guardar y continuar en la parte inferior del formulario" />
    6. Configurar los ámbitos
        1. Haga clic en "agregar o eliminar ámbitos". 
            <img src="/social-login-assets/google_cons_add_rm_sco.png" alt="Botón Agregar o eliminar ámbitos en la pantalla de configuración de ámbitos" />
        
        2. Seleccione los siguientes ámbitos y haga clic en `update`
            <img src="/social-login-assets/google_cons_update_scope.png" alt="Cuadro de diálogo de selección de ámbito con ámbitos de correo electrónico y perfil seleccionados" />
        3. Haga clic en `guardar y continuar`
            <img src="/social-login-assets/google_cons_scope_save.png" alt="Botón Guardar y continuar en la pantalla de ámbitos" />
    7. Agregue un usuario de prueba
        1. Haga clic en `agregar usuarios`
            <img src="/social-login-assets/google_cons_add_test_usr.png" alt="Botón Agregar usuarios en la sección de usuarios de prueba" />
        2. Ingrese su correo electrónico de Google, presione Enter y haga clic en `add`
            <img src="/social-login-assets/google_cons_add_test_usr_2.png" alt="Campo de correo electrónico y botón Agregar para los usuarios de prueba" />
        3. Haga clic en `guardar y continuar`
            <img src="/social-login-assets/google_cons_test_usr_save.png" alt="Botón Guardar y continuar en la pantalla de usuarios de prueba" />
    8. Haga clic en `volver al panel`
        <img src="/social-login-assets/google_cons_back_to_dahs.png" alt="Botón Volver al panel en la parte inferior de la página de finalización" />
    9. Envíe su aplicación para verificación
        :::note
            Recomiendo encarecidamente enviar la aplicación para verificación. Esto está fuera del alcance de este tutorial. Puede obtener más información [aquí](https://support.google.com/cloud/answer/13463073/). No es necesario para pruebas locales, pero sí para producción.
        :::
</Steps>

## Diferencias entre acceso online y acceso offline

Existen varias formas de usar Google Login con Capacitor. Esta tabla resume las diferencias entre ambas:

|                         | Acceso online | Acceso offline |
|:-----------------------:|:-------------:|:--------------:|
| Requiere backend        | ❌             | ✅              |
| Token de acceso duradero| ❌             | ✅              |
| Configuración sencilla  | ✅             | ❌              |

:::note
**El modo offline** significa que tu backend puede acceder a las APIs de Google incluso cuando el usuario no está usando activamente tu app. Los tokens de larga duración hacen posible esta funcionalidad al permitir que el backend llame a las APIs de Google en nombre del usuario en cualquier momento.
:::

:::caution
**El modo offline REQUIERE un servidor backend.** Cuando usas el modo offline, el frontend recibe información mínima, principalmente un código de autenticación del servidor. Sin un backend que intercambie ese código por tokens e información del usuario, el modo offline no aporta datos útiles a tu aplicación frontend.
:::

Si todavía no sabes cuál elegir, considera estos escenarios:

1. Quieres que el usuario inicie sesión y después emitirle un JWT personalizado. Tu app NO llamará a las APIs de Google

   En este caso, elige acceso online.

2. Tu app llamará a algunas APIs de Google desde el cliente, pero nunca desde el backend

   En este caso, elige acceso online.

3. Tu app llamará a algunas APIs de Google desde el backend, pero solo cuando el usuario esté usando activamente la app

   En este caso, elige acceso online.

4. Tu app comprobará periódicamente el calendario del usuario, incluso cuando no esté usando activamente la app

   En este caso, elige acceso offline.

## Un backend de ejemplo para acceso online

En esta parte del tutorial mostraré cómo validar al usuario en tu backend.

Este ejemplo será muy sencillo y estará basado en las siguientes tecnologías:

- [TypeScript](https://www.typescriptlang.org/)

- [Hono](https://hono.dev/)

- [JavaScript Fetch API](https://developer.mozilla.org/en-US/docs/Web/API/Window/fetch)

Puedes encontrar el código de este ejemplo [aquí](https://github.com/WcaleNieWolny/capgo-social-login-backend-demo/blob/141c01d93a85240e31a0d488a89df13c842708b1/index.ts#L135-L153)

Como puedes ver:

<img src="/social-login-assets/vscode_auth_google.png" alt="VS Code mostrando el código de autenticación de Google que verifica tokens" />

La idea es bastante simple. Envías una petición `GET` a `https://www.googleapis.com/oauth2/v3/tokeninfo` y eso te devuelve si el token es válido o no. Si lo es, también te da el correo electrónico del usuario y otra información sobre su token.

<img src="/social-login-assets/google_auth_playground_token_info.png" alt="Google OAuth Playground mostrando la respuesta de tokeninfo con los datos del usuario" />

A partir de ahí, puedes emitir tu propio JWT al usuario o algún tipo de cookie de sesión. Las posibilidades son amplias para la implementación final de autenticación.

Si quieres llamar a las APIs de Google, te recomiendo revisar [Google OAuth 2.0 Playground](https://developers.google.com/oauthplayground). Desde ahí puedes ver fácilmente qué APIs puedes llamar.

## Usando el acceso offline con tu propio backend

Para utilizar el acceso online necesitarás lo siguiente:

- Un servidor HTTP

En este ejemplo, utilizaré las siguientes tecnologías para proporcionar acceso offline en mi aplicación:

- [Hono](https://hono.dev/)

- [Validador Zod de Hono](https://hono.dev/docs/guides/validation#with-zod)

- [Zod](https://zod.dev/)

- [Hono JWT](https://hono.dev/docs/helpers/jwt#jwt-authentication-helper)

- [LowDb](https://www.npmjs.com/package/lowdb) (una base de datos simple)

El código de este ejemplo se puede encontrar [aquí](https://github.com/WcaleNieWolny/capgo-social-login-backend-demo/blob/aac7a8c909f650a8c2cd7f88c97f5f3c594ce9ba/index.ts#L139-L287)

En cuanto al código del cliente, se ve así:

```typescript
import { Capacitor } from '@capacitor/core';
import { GoogleLoginOfflineResponse, SocialLogin } from '@capgo/capacitor-social-login';
import { usePopoutStore } from '@/popoutStore'; // <-- specific to my app

const baseURL = "[redacted]";

async function fullLogin() {
  await SocialLogin.initialize({
    google: {
      webClientId: '[redacted]',  
      iOSClientId: '[redacted]',
      iOSServerClientId: 'The same value as webClientId',
      mode: 'offline' // <-- important
    } 
  })
  const response = await SocialLogin.login({
    provider: 'google',
    options: {
      forceRefreshToken: true // <-- important
    }
  })

  if (response.provider === 'google') {
    const result = response.result as GoogleLoginOfflineResponse
    const res = await fetch(`${baseURL}/auth/google_offline`, {
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify({
        serverAuthCode: result.serverAuthCode,
        platform: Capacitor.getPlatform()
      }),
      method: "POST"
    })

    if (res.status !== 200) {
      popoutStore.popout("Full google login failed", "check console");
      return
    }

    const { jwt } = await res.json();
    const userinfo = await fetch(`${baseURL}/auth/get_google_user`, {
      headers: {
        Authorization: `Bearer ${jwt}`
      }
    })
    if (userinfo.status !== 200) {
      popoutStore.popout("Full google (userinfo) login failed", "check console");
      return
    }
    popoutStore.popout("userinfo res", await userinfo.text());
  }
}
```
