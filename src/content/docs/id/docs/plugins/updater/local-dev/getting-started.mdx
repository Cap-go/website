---
title: "Memulai"
locale: id
description: "Tutorial ini akan menunjukkan cara memulai Supabase dari awal serta memulai edge functions, untuk menjalankan Capgo di server Anda sendiri"
sidebar:
    order: 1
---

## Apa yang akan dibahas dalam tutorial ini?

Tutorial ini akan menunjukkan cara memulai supabase dari awal serta memulai edge functions

## Persyaratan
 1. Clone [capgo](https://github.com/Cap-go/capgo)
 3. [supabase](https://supabase.com/)

## Memulai

Untuk memulai jalankan
```bash
supabase start
```

Selanjutnya Anda akan melihat sesuatu seperti ini:
```js
Started supabase local development setup.

         API URL: http://localhost:54321
     GraphQL URL: http://localhost:54321/graphql/v1
          DB URL: postgresql://postgres:postgres@localhost:54322/postgres
      Studio URL: http://localhost:54323
    Inbucket URL: http://localhost:54324
      JWT secret: [truncated]
        anon key: supa_key_anon
service_role key: supa_key_admin
```

Selanjutnya buka `configs.json` dan atur nilai berikut:
```json
{
  "base_domain": {
    "prod": "console.capgo.app",
    "development": "development.console.capgo.app",
    "local": "localhost:3332"
  },
  "supa_anon": {
    "prod": "supa_key_anon",
    "development": "supa_key_anon",
    "local": "supa_key_anon"
  },
  "supa_url": {
    "prod": "http://localhost:54321",
    "development": "http://localhost:54321",
    "local": "http://localhost:54321"
  }
}
```

di mana `supa_key_anon` adalah nilai dari langkah sebelumnya.

:::danger
⚠️ Jangan commit `configs.json` ke repo remote
:::

Selanjutnya, verifikasi bahwa Anda dapat pergi ke [localhost:54323](http://localhost:54323/projects) dan bahwa tabel `users` terlihat seperti ini

<figure><img src="/supabase.webp" alt="Dashboard Supabase menampilkan tabel users" /><figcaption></figcaption></figure>

Jika sudah, mulai edge functions dengan menjalankan:

```bash
supabase functions serve
```

dan mulai frontend dengan menjalankan:
```bash
bun run serve
```
