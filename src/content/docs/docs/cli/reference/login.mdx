---
title: login
sidebar_label: login
sidebar:
  order: 3
---

`npx @capgo/cli login [apikey]`

This command stores your Capgo API key for future use.

:::note
You can override the stored API key by passing `--apikey=<key>` to any command.
:::

Options:

- `--local`: Store the API key in the local repo and add it to `.gitignore` 
