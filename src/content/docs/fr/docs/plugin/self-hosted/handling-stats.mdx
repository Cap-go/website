---
title: Gestion des statistiques
description: Créer un endpoint de statistiques auto-hébergé
sidebar:
  order: 7
locale: fr
---

Voici un exemple de code en JavaScript pour sauvegarder les statistiques du plugin

```typescript
interface AppInfos {
  version_name: string
  action: 'delete' |
          'reset' |
          'set' |
          'set_fail' |
          'update_fail' |
          'windows_path_fail' |
          'canonical_path_fail' |
          'directory_path_fail' |
          'unzip_fail' |
          'low_mem_fail' |
          'download_fail' |
          'update_fail' |
          'download_10' |
          'download_20' |
          'download_30' |
          'download_40' |
          'download_50' |
          'download_60' |
          'download_70' |
          'download_80' |
          'download_90' |
          'download_complete'
  version_build: string
  version_code: string
  version_os: string
  plugin_version: string
  platform: string
  app_id: string
  device_id: string
  custom_id?: string
  is_prod?: boolean
  is_emulator?: boolean
}

export const handler: Handler = async (event) => {
  const body = JSONparse(eventbody || '{}') as AppInfos
  const {
    platform,
    app_id,
    action,
    version_code,
    version_os,
    device_id,
    version_name,
    version_build,
    plugin_version,
  } = body
  consolelog('update asked', platform,
    app_id,
    action,
    version_os,
    version_code,
    device_id,
    version_name,
    version_build,
    plugin_version)
  // Sauvegardez-le dans votre base de données
  return { status: 'ok' }
}
```

Ce point de terminaison doit retourner un JSON :

```json
{ "status": "ok" }
```

## Actions :

Pour des descriptions détaillées de tous les codes d'action et leurs significations, veuillez consulter la documentation de débogage :

* **Actions envoyées depuis l'appareil** : Voir la section [documentation de débogage - Envoyé depuis l'appareil](/docs/plugin/debugging/#envoyé-depuis-lappareil)
* **Actions envoyées depuis le backend** : Voir la section [documentation de débogage - Envoyé depuis le backend](/docs/plugin/debugging/#envoyé-depuis-le-backend)