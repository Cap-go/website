---
title: "Utilisation du CLI avec capgo auto-hébergé"
locale: fr
description: "Ce guide explique comment utiliser le CLI avec une instance Capgo auto-hébergée, vous permettant d'utiliser les outils Capgo sur votre propre serveur au lieu du service cloud."
sidebar:
    order: 2
---

## Que couvrira ce tutoriel ?

Ce tutoriel montrera comment utiliser le CLI dans un environnement de développement avec capgo auto-hébergé

## Démarrage

Pour utiliser le CLI avec capgo auto-hébergé, éditez le `capacitor.config.ts` depuis le répertoire de votre application et configurez-le comme ceci :

```ts
const config: CapacitorConfig = {
  appId: 'com.demo.app',
  appName: 'demoApp',
  webDir: 'dist',
  bundledWebRuntime: false,
  plugins: {
    CapacitorUpdater : {
      localHost: "http://localhost:5173",
      localWebHost: "http://localhost:5173",
      localSupa: "http://localhost:54321",
      localSupaAnon: "voir_notes",
    },
  },
};
```

Note : Pour obtenir `localSupaAnon`, veuillez suivre [ce tutoriel](/docs/plugin/self-hosted/local-dev/getting-started/) et copier la `anon key` dans `localSupaAnon`
