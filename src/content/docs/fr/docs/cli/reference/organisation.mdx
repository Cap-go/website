---
title: 🔹 organisation
description: 🏢 Gérez vos organisations dans Capgo Cloud pour la collaboration d'équipe et la gestion d'applications.
sidebar_label: organisation
locale: fr
sidebar:
  order: 9
---

🏢 Gérez vos organisations dans Capgo Cloud pour la collaboration d'équipe et la gestion d'applications.

### <a id="organisation-list"></a> 📋 **List**

**Alias :** `l`

```bash
npx @capgo/cli@latest organisation list
```

📋 Listez toutes les organisations auxquelles vous avez accès dans Capgo Cloud.

**Exemple :**

```bash
npx @capgo/cli@latest organisation list
```

**Options :**

| Param          | Type          | Description          |
| -------------- | ------------- | -------------------- |
| **-a,** | <code>string</code> | Clé API pour lier à votre compte |
| **--supa-host** | <code>string</code> | URL hôte Supabase personnalisée (pour auto-hébergement ou développement Capgo) |
| **--supa-anon** | <code>string</code> | Clé anon Supabase personnalisée (pour auto-hébergement) |

### <a id="organisation-add"></a> ➕ **Add**

**Alias :** `a`

```bash
npx @capgo/cli@latest organisation add
```

➕ Créez une nouvelle organisation dans Capgo Cloud pour la collaboration d'équipe.

**Exemple :**

```bash
npx @capgo/cli@latest organisation add --name "Mon Entreprise" --email admin@monentreprise.com
```

**Options :**

| Param          | Type          | Description          |
| -------------- | ------------- | -------------------- |
| **-n,** | <code>string</code> | Nom de l'organisation |
| **-e,** | <code>string</code> | Email de gestion pour l'organisation |
| **-a,** | <code>string</code> | Clé API pour lier à votre compte |
| **--supa-host** | <code>string</code> | URL hôte Supabase personnalisée (pour auto-hébergement ou développement Capgo) |
| **--supa-anon** | <code>string</code> | Clé anon Supabase personnalisée (pour auto-hébergement) |

### <a id="organisation-set"></a> ⚙️ **Set**

**Alias :** `s`

```bash
npx @capgo/cli@latest organisation set
```

⚙️ Mettez à jour les paramètres de l'organisation tels que le nom et l'email de gestion.

**Exemple :**

```bash
npx @capgo/cli@latest organisation set ORG_ID --name "Nom d'entreprise mis à jour"
```

**Options :**

| Param          | Type          | Description          |
| -------------- | ------------- | -------------------- |
| **-n,** | <code>string</code> | Nom de l'organisation |
| **-e,** | <code>string</code> | Email de gestion pour l'organisation |
| **-a,** | <code>string</code> | Clé API pour lier à votre compte |
| **--supa-host** | <code>string</code> | URL hôte Supabase personnalisée (pour auto-hébergement ou développement Capgo) |
| **--supa-anon** | <code>string</code> | Clé anon Supabase personnalisée (pour auto-hébergement) |

### <a id="organisation-delete"></a> 🗑️ **Delete**

**Alias :** `d`

```bash
npx @capgo/cli@latest organisation delete
```

🗑️ Supprimez une organisation de Capgo Cloud. Cette action ne peut pas être annulée.
Seuls les propriétaires d'organisation peuvent supprimer des organisations.

**Exemple :**

```bash
npx @capgo/cli@latest organisation delete ORG_ID
```

**Options :**

| Param          | Type          | Description          |
| -------------- | ------------- | -------------------- |
| **-a,** | <code>string</code> | Clé API pour lier à votre compte |
| **--supa-host** | <code>string</code> | URL hôte Supabase personnalisée (pour auto-hébergement ou développement Capgo) |
| **--supa-anon** | <code>string</code> | Clé anon Supabase personnalisée (pour auto-hébergement) |
