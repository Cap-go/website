---
locale: fr
title: Utiliser Capgo en Chine
description: "Configurez Capgo pour la Chine avec les URLs regionales adaptees afin d'ameliorer la fiabilite et les performances des mises a jour OTA."
sidebar:
  order: 11
---

import { Aside, Steps } from '@astrojs/starlight/components';

Si vous deployez votre application pour des utilisateurs en Chine, il est recommande d'utiliser les endpoints regionaux de Capgo pour des mises a jour plus stables.

## Pourquoi utiliser des URLs dediees a la Chine

Les contraintes reseau (latence, filtrage, routage international) peuvent degrad er l'acces aux infrastructures hors region. Capgo fournit des URLs optimisees pour cette zone afin de reduire les echecs et accelerer les telechargements.

## Configuration

Vous devez definir trois URLs dans `capacitor.config.ts`.

<Steps>

1. Ouvrez `capacitor.config.ts`.

2. Configurez le plugin `CapacitorUpdater`:

```typescript
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  plugins: {
    CapacitorUpdater: {
      autoUpdate: true,
      updateUrl: 'https://updater.capgo.com.cn/updates',
      statsUrl: 'https://updater.capgo.com.cn/stats',
      channelUrl: 'https://updater.capgo.com.cn/channel_self',
    },
  },
};

export default config;
```

3. Rebuild et synchronisez:

```shell
npm run build
npx cap sync
```

</Steps>

## Detail des URLs

- `updateUrl`: verification et telechargement des mises a jour
- `statsUrl`: envoi des metriques d'usage
- `channelUrl`: resolution du canal associe a l'appareil

<Aside type="tip">
Configurez les trois URLs ensemble pour garantir un fonctionnement complet.
</Aside>

## Parametres recommandes pour la Chine

### Desactiver `directUpdate`

Nous recommandons `directUpdate: false` pour eviter des experiences degradees en cas de reseau instable.

```typescript
const config: CapacitorConfig = {
  plugins: {
    CapacitorUpdater: {
      autoUpdate: true,
      directUpdate: false,
      updateUrl: 'https://updater.capgo.com.cn/updates',
      statsUrl: 'https://updater.capgo.com.cn/stats',
      channelUrl: 'https://updater.capgo.com.cn/channel_self',
    },
  },
};
```

<Aside type="caution">
Meme avec l'infrastructure optimisee, la qualite reseau peut varier. Le mode non direct est plus robuste pour la majorite des cas.
</Aside>

## Exemple complet

```typescript
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'My App',
  webDir: 'dist',
  plugins: {
    CapacitorUpdater: {
      autoUpdate: true,
      directUpdate: false,
      updateUrl: 'https://updater.capgo.com.cn/updates',
      statsUrl: 'https://updater.capgo.com.cn/stats',
      channelUrl: 'https://updater.capgo.com.cn/channel_self',
    },
  },
};

export default config;
```

## Verifier la configuration

<Steps>

1. Uploadez un bundle test:

```shell
npx @capgo/cli@latest bundle upload --channel=production
```

2. Installez l'application sur un appareil de test.

3. Surveillez le processus:

```shell
npx @capgo/cli@latest app debug
```

4. Verifiez que les requetes partent bien vers `updater.capgo.com.cn`.

</Steps>

<Aside type="note">
Le comportement d'application des mises a jour reste identique. Voir [Comportement des mises a jour](/docs/live-updates/update-behavior/).
</Aside>

## Deploiement multi-region

Si vous servez des utilisateurs en Chine et hors Chine, vous pouvez conserver ces URLs pour tous. Le domaine `.cn` est resolu globalement.

### Option unique globale

Vous pouvez utiliser une configuration unique pour tous les utilisateurs:

```typescript
const config: CapacitorConfig = {
  plugins: {
    CapacitorUpdater: {
      autoUpdate: true,
      directUpdate: false,
      updateUrl: 'https://updater.capgo.com.cn/updates',
      statsUrl: 'https://updater.capgo.com.cn/stats',
      channelUrl: 'https://updater.capgo.com.cn/channel_self',
    },
  },
};
```

Cette approche simplifie fortement l'exploitation.

### Option par region

Si vous voulez optimiser par zone geographique:

- maintenez des variantes d'app
- injectez les URLs par environnement
- segmentez par canaux

Besoin d'aide: [Support@capgo.app](mailto:Support@capgo.app) ou [Discord Communaute](https://discord.capgo.app).

## Depannage

En cas de probleme:

1. verifiez les 3 URLs dans `capacitor.config.ts`
2. testez l'acces reseau a `updater.capgo.com.cn`
3. inspectez les logs avec `npx @capgo/cli@latest app debug`
4. testez un nouvel upload pour valider la chaine complete
5. contactez [Support@capgo.app](mailto:Support@capgo.app) ou [Discord Communaute](https://discord.capgo.app)

<Aside type="caution">
N'utilisez pas les endpoints internationaux si vous ciblez ce mode de configuration regional.
</Aside>

## Etapes suivantes

- Decouvrir [Comportement des mises a jour](/docs/live-updates/update-behavior/)
- Explorer les [Canaux](/docs/live-updates/channels/)
- Configurer le [Chiffrement](/docs/live-updates/encryption/)
