---
title: Primeros pasos
description: 이 튜토리얼에서는 Supabase를 처음부터 시작하고 Edge Functions를 실행하는 방법을 보여줍니다
sidebar:
  order: 3
locale: ko
---

## 이 튜토리얼에서 다룰 내용은?

이 튜토리얼은 supabase를 처음부터 시작하는 방법과 edge functions를 시작하는 방법을 보여줄 것입니다

## 요구사항
 1 [capgo](https://githubcom/Cap-go/capgo/) 클론
 3 [supabase](https://supabasecom/)

## 시작하기

시작하려면 다음을 실행하세요
```bash
supabase start
```

다음과 같은 내용이 표시되어야 합니다:
```js
Started supabase local development setup

         API URL: http://localhost:54321
     GraphQL URL: http://localhost:54321/graphql/v1
          DB URL: postgresql://postgres:postgres@localhost:54322/postgres
      Studio URL: http://localhost:54323
    Inbucket URL: http://localhost:54324
      JWT secret: [truncated]
        anon key: supa_key_anon
service_role key: supa_key_admin
```

다음으로 `configsjson`을 열고 다음 값을 설정하세요:
```json
{
  "base_domain": {
    "prod": "webcapgoapp",
    "development": "developmentwebcapgoapp",
    "local": "localhost:3332"
  },
  "supa_anon": {
    "prod": "supa_key_anon",
    "development": "supa_key_anon",
    "local": "supa_key_anon"
  },
  "supa_url": {
    "prod": "http://localhost:54321",
    "development": "http://localhost:54321",
    "local": "http://localhost:54321"
  }
}
```

여기서 `supa_key_anon`은 이전 단계에서 얻은 값입니다

:::danger
⚠️ `configsjson`을 원격 저장소에 커밋하지 마세요
:::

다음으로 [localhost:54323](http://localhost:54323/projects/)에 접속할 수 있는지 확인하고 `users` 테이블이 다음과 같이 보이는지 확인하세요

<figure><img src="/supabasewebp" alt="supabase" /><figcaption></figcaption></figure>

확인되면 다음 명령으로 edge functions를 시작하세요:

```bash
supabase functions serve
```

그리고 다음 명령으로 프론트엔드를 시작하세요:
```bash
bun run serve
```