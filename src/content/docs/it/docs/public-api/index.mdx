---
title: Panoramica API
description: >-
  Esplora l'API pubblica di Capgo per gestire risorse come organizzazioni,
  dispositivi, canali e bundle utilizzando metodi HTTP RESTful e autenticazione.
sidebar:
  order: 1
locale: it
---

import { LinkCard } from '@astrojs/starlight/components';

Questa è la documentazione dell'API pubblica di Capgo cloud. L'API consente di gestire programmaticamente le tue risorse Capgo, incluse organizzazioni, dispositivi, canali e bundle. È progettata per essere RESTful e utilizza metodi HTTP standard.

## Autenticazione

Tutti gli endpoint API richiedono autenticazione. Per autenticare le tue richieste, aggiungi la tua chiave API nell'header `authorization`.

Esempio:
```bash
curl -H "authorization: your-api-key" https://api.capgo.app/organization/
```

<LinkCard
	title="Ottieni chiave API"
	description="Genera la tua chiave API nel dashboard di Capgo"
	href="https://console.capgo.app/dashboard/apikeys/"
/>

## Limitazione del tasso

L'API implementa la limitazione del tasso per garantire un uso equo. I limiti attuali sono:
- 100 richieste al minuto per account standard
- 1000 richieste al minuto per account aziendali

Se superi questi limiti, riceverai una risposta 429 (Too Many Requests).

## Formato della risposta

Tutte le risposte sono in formato JSON. Le risposte di successo in genere includono un oggetto `data` o un campo `status`. Le risposte di errore includono un campo `error` con una descrizione di cosa è andato storto.

Esempio di risposta di successo:
```json
{
  "status": "ok",
  "data": { ... }
}
```

Esempio di risposta di errore:
```json
{
  "error": "Invalid API key",
  "status": "KO"
}
```

## Endpoint disponibili

<LinkCard
	title="Organizzazioni"
	description="Crea e gestisci organizzazioni, aggiorna le impostazioni e gestisci le configurazioni a livello di organizzazione"
	href="/docs/public-api/organizations/"
/>

<LinkCard
	title="Chiavi API"
	description="Genera, elenca e revoca chiavi API per l'accesso sicuro all'API Capgo"
	href="/docs/public-api/api-keys/"
/>

<LinkCard
	title="Membri"
	description="Gestisci i membri dell'organizzazione, i ruoli e i permessi"
	href="/docs/public-api/members/"
/>

<LinkCard
	title="Statistiche"
	description="Accedi ad analisi dettagliate sull'utilizzo dell'app, lo storage e il consumo di larghezza di banda"
	href="/docs/public-api/statistics/"
/>

<LinkCard
	title="Canali"
	description="Controlla i canali di aggiornamento dell'app, le versioni e le politiche di aggiornamento"
	href="/docs/public-api/channels/"
/>

<LinkCard
	title="Dispositivi"
	description="Traccia e gestisci i dispositivi che eseguono la tua app, incluse le assegnazioni di versione e canale"
	href="/docs/public-api/devices/"
/>

<LinkCard
	title="Bundle"
	description="Gestisci i bundle delle app, incluso il caricamento, l'elenco e la gestione delle versioni"
	href="/docs/public-api/bundles/"
/>

## Best practice

1. **Gestione degli errori**: Controlla sempre le risposte di errore e gestiscile in modo appropriato
2. **Limitazione del tasso**: Implementa il backoff esponenziale quando si raggiungono i limiti di tasso
3. **Caching**: Memorizza in cache le risposte quando appropriato per ridurre le chiamate API
4. **Versionamento**: Tieni traccia delle modifiche API tramite il nostro changelog 
