---
title: Veröffentlichen Sie ein Live-Update
sidebar:
  order: 3
locale: de
---

import { Steps, LinkCard, Tabs, TabItem } from '@astrojs/starlight/components';

Verwende die Live-Updates-Funktion von Capgo, um die Benutzeroberfläche und die Geschäftslogik deiner App remote und in Echtzeit zu aktualisieren. Übertrage JS-Bündel-Updates direkt an deine Benutzer, ohne den App-Store zu durchlaufen, um Bugs sofort zu beheben und neue Funktionen bereitzustellen.

Dieser Leitfaden geht davon aus, dass du den [Capgo Quickstart](/docs/getting-started/quickstart) abgeschlossen hast und bereits:

<Steps>

1. Das `@capgo/capacitor-updater` SDK in deiner Capacitor-App installiert 

2. Deine App-ID und den Update-Kanal in `capacitorconfigts` konfiguriert 

3. In deinem Code die Methode `CapacitorUpdaternotifyAppReady()` hinzugefügt hast 

</Steps>

Wenn du diese Schritte noch nicht ausgeführt hast, gehe bitte zurück und schließe zuerst den Quickstart ab.

<LinkCard href="/docs/getting-started/add-an-app/" title="Eine App hinzufügen" description="Füge eine App zu deinem Capgo-Konto hinzu und installiere das Plugin in deiner App" />

## Ein Bundle hochladen

Mit dem installierten und konfigurierten Capgo SDK bist du bereit, dein erstes Live-Update-Bundle hochzuladen:

1. Baue deine Web-Ressourcen:
   ```shell
   npm run build
   ```

2. Lade das Bundle zu Capgo hoch:
    <Tabs>
      <TabItem label="Konsole">
        ```shell
      npx @capgo/cli@latest upload --channel=Production
      ```
      </TabItem>
      <TabItem label="Github Actions">
      ```yml
      // github/workflows/build_and_deploy.yml
    name: Quellcode bauen und zu Capgo senden

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    on:
      push:
        branches:
          - main

    jobs:
      deploy_to_capgo:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: 18
          - name: Abhängigkeiten installieren
            run: npm install
          - name: Bauen
            run: npm run build
          - name: Zu Capgo bereitstellen
            run: bunx @capgo/cli@latest bundle upload -a ${{ secrets.CAPGO_TOKEN }} --channel ${{ env.CHANNEL }}
            env:
              CAPGO_TOKEN: ${{ secrets.CAPGO_TOKEN }}
      ```
      </TabItem>
      <TabItem label="Gitlab">
      ```yml
      // gitlab-ci.yml
      stages:
        - build

      build:
        stage: build
        image: node:18
        cache:
          - key:
              files:
                - package-lock.json
            paths:
              - node_modules/
        script:
          - npm install
          - npm run build
          - npx @capgo/cli@latest bundle upload -a $CAPGO_TOKEN --channel $CAPGO_CHANNEL
        artifacts:
          paths:
            - node_modules/
            - dist/
        only:
          - master
      ```
      </TabItem>
    </Tabs>
   Dies wird eine neue Bundle-Version in den im Befehl angegebenen Kanal hochladen.

### Fehlersuche beim Hochladen

Wenn dein Upload fehlschlägt, überprüfe Folgendes:
- Deine App-ID in `capacitorconfig.ts` entspricht deiner App im Capgo-Dashboard
- Du führst den Upload-Befehl aus dem Stammverzeichnis deines Capacitor-Projekts aus
- Deine Web-Ressourcen sind gebaut und auf dem neuesten Stand

Wenn du weiterhin Probleme hast, gehe zum Abschnitt [Fehlersuche](/docs/getting-started/troubleshooting/).

## Empfang eines Updates auf einem Gerät

Sobald dein Bundle hochgeladen ist, kannst du das Live-Update auf einem Gerät testen:

1. Synchronisiere deine App mit dem Gerät:
   ```shell
   npx cap sync ios
   ```

2. Öffne ein weiteres Terminal und führe den folgenden Befehl aus, um den Aktualisierungsstatus zu überprüfen:
   ```shell
   npx @capgo/cli@latest app debug
   ```

3. Führe deine App lokal aus:
   ```shell
   npx cap run ios
   ``` 
   Oder öffne das iOS/Android-Projekt in Xcode/Android Studio und führe einen nativen Lauf aus.

4. Halte die App etwa 30 Sekunden lang geöffnet, damit das Update im Hintergrund heruntergeladen werden kann.

5. Die Protokolle benötigen einige Sekunden, um sich zu aktualisieren und den Aktualisierungsstatus anzuzeigen.

6. Schließe die App und öffne sie erneut. Du solltest dein Live-Update sehen!

Siehe zurück zum [Capgo Quickstart](/docs/getting-started/quickstart#receiving-a-live-update-on-a-device) für weitere Einzelheiten zum Testen von Live-Updates.

## Nächste Schritte

Herzlichen Glückwunsch zum Bereitstellen deines ersten Live-Updates mit Capgo! 🎉 

Um mehr zu erfahren, siehe dir den Rest der [Capgo Live Updates-Dokumentation](/docs/live-updates) an. Einige wichtige Themen, die du als Nächstes überprüfen solltest:
- [Zielgerichtete Updates mit Kanälen](/docs/live-updates/channels)
- [Anpassen des Aktualisierungsverhaltens](/docs/live-updates/update-behavior)
- [Rückrolls von Live-Updates](/docs/live-updates/rollbacks)