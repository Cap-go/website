---
title: Anwendung
sidebar_label: app
sidebar:
  order: 4
locale: de
---

Der `app` Befehl ermöglicht es Ihnen, Ihre Capgo-Apps zu verwalten.

### hinzufügen

`npx @capgo/cli app add [appId]`

Fügt eine neue App zu Ihrem Capgo-Konto hinzu.

`[appId]` ist Ihre App-ID im Format `comexampleapp`. Siehe die [Capacitor-Dokumentation](https://capacitorjscom/docs/cli/commands/init/) für weitere Informationen.

> 💡 Alle Optionen werden aus Ihrer `capacitorconfigjson` erraten, wenn sie nicht angegeben werden.

Optionen:

- `--icon [path]`: Pfad zu einem benutzerdefinierten Symbol, das in der Capgo-Web-App angezeigt werden soll.
- `--name [name]`: Benutzerdefinierter Name, der in der App-Liste angezeigt werden soll.
- `--apikey [key]`: API-Schlüssel, um mit Ihrem Konto zu verknüpfen.
- `--retention [days]`: Aufbewahrungsdauer für App-Pakete in Tagen (Standard: 0 = unendlich).

### festlegen

`npx @capgo/cli app set [appId]`

Aktualisiert eine vorhandene App in Ihrem Capgo-Konto.

Optionen:

- `--icon [path]`: Pfad zu einem benutzerdefinierten Symbol, das in der Capgo-Web-App angezeigt werden soll.
- `--name [name]`: Benutzerdefinierter Name, der in der App-Liste angezeigt werden soll.
- `--retention [days]`: Aufbewahrungsdauer für App-Pakete in Tagen (Standard: 0 = unendlich).
- `--apikey [key]`: API-Schlüssel, um mit Ihrem Konto zu verknüpfen.

### liste

`npx @capgo/cli app list [appId]`

Listet alle Apps in Ihrem Capgo-Konto auf.

Optionen:

- `--apikey [key]`: API-Schlüssel, um mit Ihrem Konto zu verknüpfen.

### löschen

`npx @capgo/cli app delete [appId]`

Löscht eine App aus Ihrem Capgo-Konto.

Optionen:

- `--apikey [key]`: API-Schlüssel, um mit Ihrem Konto zu verknüpfen.
- `--bundle`: Löscht nur eine spezifische Bundle-Version.

### debuggen

`npx @capgo/cli app debug [appId]`

Zeigt Debug-Informationen für eine App an.

Optionen:

- `--apikey [key]`: API-Schlüssel, um mit Ihrem Konto zu verknüpfen.
- `--device`: Debuggen eines spezifischen Geräts.

### einstellung

`npx @capgo/cli app setting [path]`

Bearbeitet die Capacitor-Konfiguration für eine App.

`[path]` ist der Pfad zu der Einstellung, die Sie ändern möchten (z.B. `appId` oder `pluginsCapacitorUpdaterautoUpdate`).

Sie müssen entweder `--string` oder `--bool` angeben:

- `--string <value>`: Setzt die Einstellung auf einen String-Wert.
- `--bool <true|false>`: Setzt die Einstellung auf einen booleschen Wert.