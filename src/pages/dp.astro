---
import VueDataPolicy from '@/vue-pages/data_policy.vue'
import Layout from '@/layouts/Layout.astro'
import { useRuntimeConfig } from '@/config/app'
import { defaultLocale } from '@/services/locale'

const config = useRuntimeConfig()

const brand = config.public.brand

const title = `${brand} | Data Policy`
const description = 'Data Policy'

const content = {
  description,
  title,
}
---

<Layout content={content}><VueDataPolicy locale={Astro.currentLocale || defaultLocale} client:load /></Layout>
