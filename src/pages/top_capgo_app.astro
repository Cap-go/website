---
import Layout from '@/layouts/Layout.astro'
import VueTopcapgoapp from '@/vue-pages/top_capgo_app.vue'
import translations from '@/services/translations'
import { useRuntimeConfig } from '@/config/app'

const config = useRuntimeConfig()
const brand = config.public.brand

const title = [brand, translations['top_capgo_apps_title'][Astro.locals.locale]].join(' | ')
const description = translations['top_capgo_apps'][Astro.locals.locale]

const content = { title, description }
---

<Layout content={content}><VueTopcapgoapp locale={Astro.locals.locale} client:load /></Layout>
