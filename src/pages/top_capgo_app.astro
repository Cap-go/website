---
import Layout from '@/layouts/Layout.astro'
import { defaultLocale } from '@/services/locale'
import VueTopcapgoapp from '@/vue-pages/top_capgo_app.vue'

const title = 'Capgo | Top Capgo apps'
const description = 'List of top app using Capgo'

const content = {
  description,
  title,
}
---

<Layout content={content}><VueTopcapgoapp locale={Astro.currentLocale || defaultLocale} client:load /></Layout>
