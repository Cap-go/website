---
import Layout from '@/layouts/Layout.astro'
import { defaultLocale, type Locales } from '@/services/locale'
import VueRegister from '@/vue-pages/register.vue'
---

<Layout><VueRegister locale={(Astro.currentLocale || defaultLocale) as Locales} client:load /></Layout>
