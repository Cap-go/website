---
import Layout from '@/layouts/Layout.astro'
import VuePrivacy from '@/vue-pages/privacy.vue'

import { useRuntimeConfig } from '@/config/app'
import { defaultLocale } from '@/services/locale'

const config = useRuntimeConfig()

const brand = config.public.brand

const title = `${brand} | Privacy Policy`
const description = 'Read our Privacy Policy'

const content = {
  description,
  title,
}
---

<Layout content={content}><VuePrivacy locale={Astro.currentLocale || defaultLocale} client:load /></Layout>
