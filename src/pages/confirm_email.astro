---
import Layout from '@/layouts/Layout.astro'
import { defaultLocale } from '@/services/locale'
import VueConfirmEmail from '@/vue-pages/confirm_email.vue'
---

<Layout><VueConfirmEmail locale={Astro.currentLocale || defaultLocale} client:load /></Layout>
