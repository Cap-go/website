diff --git a/dist/core/build/plugins/plugin-css.js b/dist/core/build/plugins/plugin-css.js
index 27ef939a603855cb2ab4528e35589ad9bc9e36d8..f86d0177477960a8801402151e63778eabbf272f 100644
--- a/dist/core/build/plugins/plugin-css.js
+++ b/dist/core/build/plugins/plugin-css.js
@@ -15,6 +15,11 @@ function pluginCSS(options, internals) {
     internals
   });
 }
+function isBuildCssBoundary(id, ctx) {
+  if (hasAssetPropagationFlag(id)) return true;
+  const info = ctx.getModuleInfo(id);
+  return info ? moduleIsTopLevelPage(info) : false;
+}
 function rollupPluginAstroBuildCSS(options) {
   const { internals, buildOptions } = options;
   const { settings } = buildOptions;
@@ -95,7 +100,7 @@ function rollupPluginAstroBuildCSS(options) {
                 const parentModuleInfos = getParentExtendedModuleInfos(
                   scopedToModule,
                   this,
-                  hasAssetPropagationFlag
+                  (id) => isBuildCssBoundary(id, this)
                 );
                 for (const { info: pageInfo, depth, order } of parentModuleInfos) {
                   if (moduleIsTopLevelPage(pageInfo)) {
@@ -152,7 +157,11 @@ function rollupPluginAstroBuildCSS(options) {
         }
         for (const id of Object.keys(chunk.modules)) {
           if (!isCSSRequest(id)) continue;
-          const parentModuleInfos = getParentExtendedModuleInfos(id, this, hasAssetPropagationFlag);
+          const parentModuleInfos = getParentExtendedModuleInfos(
+            id,
+            this,
+            (importer) => isBuildCssBoundary(importer, this)
+          );
           for (const { info: pageInfo, depth, order } of parentModuleInfos) {
             if (hasAssetPropagationFlag(pageInfo.id)) {
               const propagatedCss = moduleIdToPropagatedCss[pageInfo.id] ??= /* @__PURE__ */ new Set();
